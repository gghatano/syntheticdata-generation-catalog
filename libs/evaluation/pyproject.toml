[project]
name = "synth-evaluation"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "sdmetrics",
    "pandas",
    "numpy",
    "scikit-learn",
    "matplotlib",
    "seaborn",
]

[tool.uv]
package = false
