[project]
name = "synth-ydata"
version = "0.1.0"
requires-python = ">=3.9,<3.12"
dependencies = [
    "ydata-synthetic",
    "pandas",
    "numpy",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = []
