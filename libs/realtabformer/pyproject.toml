[project]
name = "synth-realtabformer"
version = "0.1.0"
requires-python = ">=3.10,<3.12"
dependencies = [
    "realtabformer",
    "pandas",
    "numpy",
    "torch",
    "transformers>=4.46,<5",
    "accelerate",
]

[tool.uv]
package = false
